import {
  application
} from '../../../helpers/application';
import { module, test } from 'qunit';

module('ApplicationHelper');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = application(42);
  assert.ok(result);
});
